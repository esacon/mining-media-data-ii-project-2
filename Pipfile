[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Core ML/Data Science
torch = ">=2.7.0"
transformers = ">=4.42.0,<4.55.0"
scikit-learn = ">=1.7.0"
pandas = ">=2.3.0"
numpy = ">=2.3.0"
tqdm = ">=4.67.0"

# Configuration
pyyaml = ">=6.0.0"

# Development Tools
black = ">=25.1.0"
flake8 = ">=7.3.0"
isort = {extras = ["colors"], version = ">=6.0.1"}
mypy = ">=1.16.0"
pre-commit = ">=4.2.0"
pytest = ">=8.4.0"
autoflake = ">=2.3.0"
pyupgrade = ">=3.20.0"

# LLM support (optional - for memory efficiency)
accelerate = ">=0.20.0"
bitsandbytes = ">=0.41.0"

# Runtime dependencies (automatically resolved)
tokenizers = ">=0.21.0"
safetensors = ">=0.5.0"
requests = ">=2.32.0"
huggingface-hub = ">=0.33.0"

[dev-packages]

[requires]
python_version = "3.11"
